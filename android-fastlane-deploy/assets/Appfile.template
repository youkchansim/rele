# Appfile for Android
# Generated by android-fastlane-deploy
#
# Credentials are managed in Fastfile via ~/.playstore_keys/config.json
# (project local fallback: fastlane/*.json for CI/CD)

for_platform :android do
  package_name("{{PACKAGE_NAME}}")
end
