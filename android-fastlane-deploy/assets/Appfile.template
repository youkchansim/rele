# Appfile for Android
# Generated by android-fastlane-deploy

# Package name (from build.gradle applicationId)
package_name("{{PACKAGE_NAME}}")

# Google Play credentials
# Option 1: Global shared folder (recommended)
json_key_file("#{ENV['HOME']}/.google_play_keys/api-key.json")

# Option 2: Project-specific file
# json_key_file("fastlane/play-store-credentials.json")

# Option 3: Environment variable
# json_key_file(ENV['GOOGLE_PLAY_KEY_FILE'])
