# Appfile Template for iOS App Store Deployment
#
# 사용법:
#   1. 이 파일을 프로젝트의 fastlane/Appfile로 복사
#   2. 아래 값들을 실제 프로젝트 정보로 수정

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🔧 프로젝트 설정 (수정 필요)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

app_identifier("com.yourcompany.app")  # Bundle Identifier
apple_id("your-email@example.com")     # Apple ID (App Store Connect 계정)
team_id("XXXXXXXXXX")                  # Developer Portal Team ID

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 📝 참고 사항
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Team ID 확인 방법:
#   1. https://developer.apple.com/account 접속
#   2. Membership 페이지에서 Team ID 확인
#
# 또는 터미널에서:
#   security find-identity -v -p codesigning

# ⚠️ 중요: app_store_connect_api_key는 Fastfile에서 설정합니다!
# Appfile에서는 사용할 수 없는 메서드입니다.
